import Component from '@ember/component';

export default Component.extend({
  actions: {
    deleteProduct() {
      this.deleteProduct();
    },

    updateProduct(){
      this.updateProduct();
    }
  }
});
