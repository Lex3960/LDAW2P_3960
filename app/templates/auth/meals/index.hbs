<div class="row">
  <div class="col s10 offset-s1">
  <blockquote><h3>Meals</h3></blockquote>
    <form {{action 'createMeal' on="submit"}}>
          Set the name:
          <div class="input-field">
            {{input type="text" value=name}}
            <label>Name</label>
          </div>
      <button class="waves-effect waves-light blue darken-2 btn">Create</button>
    </form>
  </div>
</div>
<div class="row">
  <div class="col s10 offset-s1">
    <div class="row">
      {{#each model as |meal|}}
        {{#simple-card item=meal as |mealName edit delete|}}
          <div class="col s3">
            <div class="card orange darken-3">
              <div class="card-image">
              <img src="https://31.media.tumblr.com/tumblr_lk7si31zPt1qdub50.gif">
              <span class="card-title">{{mealName}}</span>
            </div>
              <div class="card-action">
              {{#link-to 'auth.meals.detail' meal.id class="white-text"}}
                {{edit}}
              {{/link-to}}
              <button class="waves-effect waves-light red btn" {{action 'deleteMeal' meal}}>
                {{delete}}
              </button>
              </div>
            </div>
          </div>
        {{/simple-card}}
      {{/each}}
    </div>
  </div>
</div>
